# App API Endpoints

This controller serves root-level endpoints.

## Auth and access

- Public endpoint, no auth required.

## Endpoints

### GET `/`

Returns a simple text response.

- Auth: none.
- Response: `200 OK` with plain text `Hello World!`.
